const accessKeyId = process.env.ACCESS_KEY_ID;
const secretAccessKey = process.env.SECRET_ACCESS_KEY;
const awsRegion = process.env.AWS_REGION;
const fromEmail = "notificaciones@dipromacom.net";

module.exports = {
  accessKeyId,
  secretAccessKey,
  awsRegion,
  fromEmail,
};
